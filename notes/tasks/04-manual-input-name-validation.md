# タスク04: 手動入力で艦娘名未入力時の追加を防止

## 概要
手動入力画面で名前を空のまま「追加」ボタンを押すと、選択中の艦級名（「駆逐」「軽巡」など）で登録されてしまう。実用上意味のない登録なので防止する。

## 対象ファイル
- `src/ExpeditionProposer.vue`

## 対応内容
- 艦娘名が未入力（空文字・空白のみ）の場合は「追加」ボタンを無効化（`disabled`）するか、押下時にバリデーションエラーを表示する
- 追加で検討: 名前欄をプルダウン（艦種に連動した艦娘名候補）にして誤入力を減らす

## 優先度
高（データ不整合防止）
